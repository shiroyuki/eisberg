# Eisberg - Simplified API for Apache Iceberg

This is made on top of `pyiceberg`.

## Prerequisites

* `pyiceberg`
  * **NOTICE:** Due to various options on how to install `pyiceberg` with optional dependencies, this is not installed automatically.
